# GitHub Personal Access Token
# Optional: without it, mock data is used (sufficient for development)
# Generate at: https://github.com/settings/tokens (no scopes needed - only public data)
#
# Setup: cp .env.template .env && edit .env
GITHUB_TOKEN=
